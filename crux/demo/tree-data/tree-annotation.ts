export default `//bvt
svg {
    height = 1100;
    width = auto;
    Component {
        TreeAnnotation {
            treeData = treeData;
            colAnnotation = colAnnotation;
            treeAnnotation = treeAnnotation;
            columnNames = columnNames;
            id = id;
            treeWidth = treeWidth;
            treeHeight = treeHeight;
            treeAnnotationMargin = treeAnnotationMargin;
            scaleBranchLength = scaleBranchLength;
            annotationBorderColor = annotationBorderColor;
            treeAnnoKey = treeAnnoKey;
        }
    }
}
`;
