export default `//bvt
svg {
    width = auto;
    height = 1000;
    CircularHeatTree {
        treeData = treeData;
        heatData = heatData;
        annotationData = annotationData;
        annotationKey = annotationKey;
        idKey = idKey;
        colorShare = colorShare;
    }
}
`;
